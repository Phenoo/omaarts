rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper functions for user authorization
    function getUserData() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
    }
    
    function isAdmin() {
      return request.auth != null && 
        (getUserData().role == 'admin' || getUserData().role == 'super_admin');
    }
    
    function isStaff() {
      return request.auth != null && 
        (getUserData().role == 'admin' || getUserData().role == 'super_admin' || getUserData().role == 'staff');
    }

    function isOwner(userId) {
      return request.auth != null && request.auth.uid == userId;
    }

    // Users collection rules
    match /users/{userId} {
      allow read: if request.auth != null && (request.auth.uid == userId || isStaff());
      allow write: if request.auth != null && (request.auth.uid == userId || isAdmin());

      // Wishlist subcollection — only the owning user can read/write
      match /wishlist/{artworkId} {
        allow read, write: if isOwner(userId);
      }

      // Cart subcollection — only the owning user can read/write
      match /cart/{docId} {
        allow read, write: if isOwner(userId);
      }
    }

    // Activities collection rules
    match /activities/{activityId} {
      allow read: if true;
      allow write: if isStaff();
    }

    // Artworks collection rules
    match /artworks/{artworkId} {
      allow read: if true;
      allow write: if isStaff();
    }

    // Categories collection rules
    match /categories/{categoryId} {
      allow read: if true;
      allow write: if isStaff();
    }

    // Bookings collection rules
    match /bookings/{bookingId} {
      // Guests can create bookings; logged-in users can read their own; staff can read all
      allow read: if true; // anyone who knows the secure ID can view their receipt
      allow create: if true; // anyone can book
      allow update, delete: if isStaff();
    }

    // Orders collection rules
    match /orders/{orderId} {
      // Guests can create orders; logged-in users can read their own; staff can read all
      allow read: if true; // anyone who knows the secure ID can view their receipt
      allow create: if true; // guest checkout supported
      allow update, delete: if isStaff();
    }

    // Payments collection rules
    match /payments/{paymentId} {
      allow read: if isStaff();
      allow write: if isAdmin();
    }

    // Sales ledger rules
    match /sales/{saleId} {
      allow read, write: if isStaff();
    }

    // Blocked Dates rules
    match /blockedDates/{id} {
      allow read: if true;
      allow write: if isStaff();
    }

    // Enquiries collection rules
    match /enquiries/{enquiryId} {
      allow read: if isStaff();
      allow create: if true; // anyone can send contact/event enquiries
      allow update, delete: if isStaff();
    }

    // Testimonials collection rules
    match /testimonials/{id} {
      allow read: if true;
      allow write: if isStaff();
    }

    // Gallery collection rules
    match /gallery/{id} {
      allow read: if true;
      allow write: if isStaff();
    }

    // Site settings collection rules
    match /siteSettings/{settingId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Inventory movements log rules
    match /inventoryMovements/{movementId} {
      allow read, write: if isStaff();
    }

    // Activity Price changes rules
    match /activityPriceHistory/{historyId} {
      allow read: if isStaff();
      allow write: if isAdmin();
    }

    // Security Audit logs rules
    match /auditLogs/{logId} {
      allow read: if isStaff();
      allow write: if isAdmin();
    }
  }
}

