import ArtworkDetailPage from '../../portfolio/[slug]/page';

// Re-use the identical presentation layout to avoid duplicating code.
export default ArtworkDetailPage;

export const dynamic = 'force-dynamic';
export const dynamicParams = true;
